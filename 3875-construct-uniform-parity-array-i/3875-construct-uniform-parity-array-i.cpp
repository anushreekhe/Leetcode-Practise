class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // int x=nums1[0];
        // if(x%2==0) f=0;
        // else f=1;
        // for(int i=1;i<nums.size();i++){
        //     if(nums[i]%2==f) continue;
        //     else nums[i]=nums[i]-x;
        // }
        return true;
    }
};